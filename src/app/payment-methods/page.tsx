import Link from "next/link";
import { redirect } from "next/navigation";
import { IconCreditCard, IconShieldCheck } from "@tabler/icons-react";
import { getSessionUser } from "@/lib/auth";
import BottomNav from "@/components/BottomNav";
export default async function PaymentMethodsPage() { if (!(await getSessionUser())) redirect("/login?next=/payment-methods"); return <div className="mx-auto min-h-dvh max-w-md bg-white pb-20"><header className="px-4 pb-3 pt-5"><Link href="/login" className="text-sm font-semibold text-primary">← Профиль</Link><h1 className="mt-3 text-2xl font-bold">Способы оплаты</h1></header><main className="space-y-4 px-4"><section className="rounded-[18px] bg-primary p-5 text-white"><IconCreditCard size={34} /><h2 className="mt-4 text-lg font-bold">Оплата онлайн при заказе</h2><p className="mt-1 text-sm text-white/80">Карта выбирается на защищённой странице платёжного провайдера.</p></section><section className="rounded-[17px] border p-4"><p className="flex gap-2 text-sm"><IconShieldCheck className="shrink-0 text-primary" size={20} /><span><b>FoodGood не хранит данные карты.</b><br /><span className="text-muted">Сначала сумма резервируется, а списывается только после выдачи заказа.</span></span></p></section><p className="text-center text-xs text-muted">Сохранённые карты появятся после подключения Freedom Pay.</p></main><BottomNav /></div>; }
