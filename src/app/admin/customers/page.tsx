import Link from "next/link";
import { Prisma } from "@prisma/client";
import { prisma } from "@/lib/db";
export default async function AdminCustomersPage({ searchParams }: { searchParams: Promise<{ q?: string }> }) { const query = ((await searchParams).q ?? "").trim(); const where: Prisma.UserWhereInput = { role: "CUSTOMER", ...(query ? { OR: [{ phone: { contains: query } }, { name: { contains: query, mode: "insensitive" } }] } : {}) }; const users = await prisma.user.findMany({ where, include: { _count: { select: { orders: true, reviews: true, favorites: true } } }, orderBy: { createdAt: "desc" }, take: 100 }); return <main className="mx-auto max-w-5xl space-y-5 p-4 sm:p-6"><header><h1 className="text-2xl font-bold">Покупатели</h1><p className="mt-1 text-sm text-muted">История заказов, отзывы и управление доступом.</p></header><form className="flex gap-2 rounded-2xl border bg-white p-4"><input name="q" defaultValue={query} placeholder="Имя или телефон" className="min-w-0 flex-1 rounded-xl border px-3 py-2.5" /><button className="rounded-xl bg-primary px-5 font-semibold text-white">Найти</button></form><section className="overflow-hidden rounded-2xl border bg-white">{users.length === 0 ? <p className="p-8 text-center text-sm text-muted">Покупатели не найдены.</p> : <div className="divide-y">{users.map((user) => <Link key={user.id} href={`/admin/customers/${user.id}`} className="flex items-center justify-between gap-3 p-4 hover:bg-black/[0.02]"><div><p className="font-semibold">{user.name ?? "Без имени"}</p><p className="text-sm text-muted">{user.phone ?? "Без телефона"}</p><p className="mt-1 text-xs text-muted">{user._count.orders} заказов · {user._count.favorites} избранных · {user._count.reviews} отзывов</p></div><span className={`rounded-full px-2.5 py-1 text-xs font-semibold ${user.status === "BLOCKED" ? "bg-red-50 text-red-700" : "bg-green-50 text-green-700"}`}>{user.status === "BLOCKED" ? "Заблокирован" : "Активен"}</span></Link>)}</div>}</section></main>; }
