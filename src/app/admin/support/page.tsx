import Link from "next/link";
import { prisma } from "@/lib/db";
import AdminResolveSupportButton from "@/components/AdminResolveSupportButton";
export default async function AdminSupportPage() { const orders = await prisma.order.findMany({ where: { supportStatus: "OPEN" }, include: { user: true, bag: { include: { venue: true } } }, orderBy: { createdAt: "asc" }, take: 100 }); return <main className="mx-auto max-w-5xl space-y-5 p-4 sm:p-6"><header><h1 className="text-2xl font-bold">Обращения покупателей</h1><p className="mt-1 text-sm text-muted">Спорные заказы, требующие решения администратора.</p></header><section className="overflow-hidden rounded-2xl border bg-white">{orders.length === 0 ? <p className="p-8 text-center text-sm text-muted">Открытых обращений нет.</p> : <div className="divide-y">{orders.map((order) => <article key={order.id} className="space-y-3 p-4"><div className="flex flex-wrap justify-between gap-3"><div><p className="font-semibold">{order.bag.venue.name} · {order.bag.title}</p><p className="text-xs text-muted">{order.user.phone ?? order.user.name ?? "Покупатель"} · код {order.pickupCode}</p></div><span className="rounded-full bg-amber-50 px-2.5 py-1 text-xs font-semibold text-amber-800">{order.status}</span></div><p className="rounded-xl bg-black/[0.03] p-3 text-sm">{order.supportNote}</p><div className="flex items-center justify-between"><Link href={`/admin/orders?q=${order.id}`} className="text-xs font-semibold text-primary">Открыть заказ</Link><AdminResolveSupportButton id={order.id} /></div></article>)}</div>}</section></main>; }
